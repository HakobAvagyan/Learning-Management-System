apiVersion: 1

datasources:
  - name:    Loki
    type:    loki
    uid:     loki
    access:  proxy
    url:     http://loki:3100
    isDefault: true
    jsonData:
      maxLines:    5000
      timeout:     60
      httpHeaderName1: "X-Scope-OrgID"
    secureJsonData:
      httpHeaderValue1: "tenant1"
    version:   1
    editable:  true